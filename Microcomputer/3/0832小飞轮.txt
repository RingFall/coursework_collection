CODE SEGMENT
ASSUME CS:CODE

START:
MOV AL,0FFH		;初值为FFH
AGAIN: 
  INC AL			;AL增1,到最大值时自动重新降为00H
  MOV DX,280H
  OUT DX,AX		;D/A转换结束,输出电压到直流电机

  MOV BX,0008H		;二重循环延时
  DELAY1:MOV CX,16EAH
  DELAY:LOOP DELAY
  DEC BX
  JNZ DELAY1

 JMP AGAIN		;重复制造矩形波

CODE ENDS
END START